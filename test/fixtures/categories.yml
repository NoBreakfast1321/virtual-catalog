# == Schema Information
#
# Table name: categories
#
#  id          :integer          not null, primary key
#  description :text(150)
#  name        :string(30)       not null
#  visible     :boolean          default(TRUE), not null
#  created_at  :datetime         not null
#  updated_at  :datetime         not null
#  catalog_id  :integer          not null
#
# Indexes
#
#  index_categories_on_catalog_id           (catalog_id)
#  index_categories_on_catalog_id_and_name  (catalog_id,name) UNIQUE
#
# Foreign Keys
#
#  catalog_id  (catalog_id => catalogs.id) ON DELETE => cascade
#
<% count = 20 %>

<% Faker::Commerce.unique.clear %>

<% count.times do |i| %>
  <% name = Faker::Commerce.unique.department(max: 1, fixed_amount: true)[0..29] %>

  category_<%= i + 1 %>:
    name: <%= name %>
    catalog: catalog_1
<% end %>

<% Faker::Commerce.unique.clear %>

<% count.times do |i| %>
  <% name = Faker::Commerce.unique.department(max: 1, fixed_amount: true)[0..29] %>

  category_<%= i + 1 + count %>:
    name: <%= name %>
    catalog: catalog_2
<% end %>
