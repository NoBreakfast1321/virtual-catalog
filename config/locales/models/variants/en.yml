en:
  activerecord:
    errors:
      models:
        variant:
          attributes:
            primary:
              no_property_group: "product must have at least one property group"
              single_primary_variant: "only one primary variant can be created"
            property_ids:
              must_select_property: "You must select a property."
