<%= render_toast %>

<%= turbo_stream.replace turbo_frame_id(:variants) do %>
  <%= render partial: "products/variants", locals: { product: @product } %>
<% end %>

<%= turbo_stream.append turbo_frame_id(@property_group, :properties), partial: "properties/property", locals: { property: @property } %>

<%= turbo_stream.update turbo_frame_id(@property_group, Property.new), "" %>
