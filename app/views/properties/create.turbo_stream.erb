<%= render_toast %>

<%= turbo_stream.append turbo_frame_id(:properties) do %>
  <%= render "properties/property",
         catalog: @catalog,
         property_group: @property_group,
         property: @property %>
<% end %>

<%= turbo_stream.update turbo_frame_id(Property.new), "" %>
