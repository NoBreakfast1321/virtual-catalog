<%= render_toast %>

<% @secondary_variants.each do |secondary_variant| %>
  <%= turbo_stream.remove turbo_frame_id(secondary_variant) %>
<% end %>

<%= turbo_stream.remove turbo_frame_id(@property) %>
