<%= render_toast %>

<%= turbo_stream.replace turbo_frame_id(@property) do %>
  <%= render "properties/property",
         catalog: @catalog,
         property_group: @property_group,
         property: @property %>
<% end %>
