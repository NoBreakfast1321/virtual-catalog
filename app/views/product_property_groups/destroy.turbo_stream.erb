<%= render_toast %>

<%= turbo_stream.remove turbo_frame_id(@product_property_group) %>

<%= turbo_stream.replace turbo_frame_id(:variants_section) do %>
  <%= render "products/variants_section", catalog: @catalog, product: @product %>
<% end %>
