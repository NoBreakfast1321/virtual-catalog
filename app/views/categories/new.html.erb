<% content_for :title, "New Category" %>

<section class="card">
  <header class="card__header">
    <h2 class="card__heading">New Category</h2>
    <p class="card__description">
      Fill in the fields below to create a new category.
    </p>
  </header>

  <div class="card__body">
    <%= render "categories/form", business: @business, category: @category %>
  </div>
</section>
