<% content_for :title, "Editing Category" %>

<section class="card">
  <header class="card__header">
    <h2 class="card__heading">Editing Category</h2>
    <p class="card__description">Update your category details below.</p>
  </header>

  <div class="card__body">
    <%= render "categories/form", business: @business, category: @category %>
  </div>
</section>
