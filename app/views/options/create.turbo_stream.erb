<%= render_toast %>

<%= turbo_stream.append turbo_frame_id(@option_group, :options), partial: "options/option", locals: { option: @option } %>

<%= turbo_stream.update turbo_frame_id(@option_group, Option.new), "" %>
