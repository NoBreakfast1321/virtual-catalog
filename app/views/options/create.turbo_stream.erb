<%= render_toast %>

<%= turbo_stream.append turbo_frame_id(:options) do %>
  <%= render "options/option",
         business: @business,
         option_group: @option_group,
         option: @option %>
<% end %>

<%= turbo_stream.update turbo_frame_id(Option.new), "" %>
