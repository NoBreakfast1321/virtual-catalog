<%= render_toast %>

<%= turbo_stream.replace turbo_frame_id(@option) do %>
  <%= render "options/option",
         catalog: @catalog,
         option_group: @option_group,
         option: @option %>
<% end %>
